# TaskManager frontend

React + Vite + TypeScript SPA for the TaskManager API.

```bash
npm ci
npm run dev
```

Optional: copy `.env.example` to `.env` and set `VITE_API_URL` (default `http://localhost:5080`).
